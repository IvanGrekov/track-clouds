services:
  telegram-monitor:
    build: .
    restart: unless-stopped
    stop_grace_period: 15s
    env_file:
      - .env
    volumes:
      - telegram-monitor-state:/app/.state

volumes:
  telegram-monitor-state:
