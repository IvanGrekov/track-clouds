# Create these credentials at https://my.telegram.org/apps
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=replace_with_your_api_hash

# Generate a dedicated string with: telegram-monitor generate-session
# Treat it like a password. Do not reuse a session that is running in another process.
TELEGRAM_SESSION_STRING=replace_with_a_dedicated_session_string

# Optional. Required only when config.toml has notification_mode = "bot".
# Create a bot with @BotFather. After the monitor starts, every recipient must
# open that bot and send /start. The first 10 users will be subscribed.
TELEGRAM_BOT_TOKEN=replace_with_your_bot_token

# Optional logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Optional. Defaults to config.toml in the current working directory.
# MONITOR_CONFIG_FILE=~/.config/telegram-monitor/config.toml

# Required when [ai_observation].enabled = true or for `ai-check --live`.
# Create a project-scoped key in OpenAI Platform.
OPENAI_API_KEY=replace_with_your_openai_project_api_key

# On Railway, create this as a separate multiline service variable and paste the
# complete private policy-prompt.txt content. Locally, leave it unset so the app
# uses the ignored policy-prompt.txt file configured by policy_prompt_path.
# AI_POLICY_PROMPT="Paste the complete current private policy here as multiline text."

# Optional. On Railway, paste the separate extended-examples section as another
# multiline variable. For a local direct run, leave it unset to use the ignored file
# configured by policy_prompt_extended_examples_path when that file exists and is non-empty.
# Compose does not mount that optional file; pass this variable when using Compose.
# An explicitly empty variable disables the extension without falling back to the file.
# AI_POLICY_PROMPT_EXTENDED_EXAMPLES="Paste optional extended examples here."
